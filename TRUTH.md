# TRUTH.md — SLOPOS-I Factual Audit & Readiness Ledger

**Audit date:** 2026-08-13
**Branch:** `pivot`
**Audit scope:** remediation working tree after the X11 packaging, catalogue, shell, QA, AT-SPI, browser-launch and bounded DBusMenu importer tranche
**Current evidence-backed readiness:** **78/100**

The previous `100/100` claim was invalid. This audit credits the fixes that have objective source, test, Docker/Xvfb, installed-VM and live-ISO/QEMU evidence, while withholding credit for independent visual acceptance, hardware-backed service/accessibility, independent localization review and hardware-target performance evidence that has not been produced.

## Scorecard

| Domain | Weight | Score | Evidence-based status |
|---|---:|---:|---|
| System 7 / Platinum visual identity | 20 | **12/20** | Fresh 1280x800 captures now show the slate desktop, Platinum bar, framed Application Strip, packaged Settings icon set and coherent shell windows. The visual gate is still below the contract because upstream-window leakage and several canonical states lack independent review. |
| Desktop shell / interaction | 15 | **11/15** | Top bar, live focus/time/status updates, singleton Search, keyboard navigation, explicit unavailable controls and local notifications are covered by source/tests and Xvfb interaction. The former fabricated focused-window File/Edit/View/Window/Help proxy is removed. X11 AppMenu properties (`_GTK_UNIQUE_BUS_NAME` plus `_GTK_APP_MENU_OBJECT_PATH` or `_GTK_MENUBAR_OBJECT_PATH`) now enable a bounded off-thread DBusMenu layout importer with protocol-only click events; malformed layouts, missing buses and timeouts fall back to the upstream local menu. The Docker harness now contains a disposable QA-only `libdbus-1` exporter fixture and hard-fail switch for a real `GetLayout`/`Event` click-through, but that leg is not rerun in the current cache-less environment; prior evidence only detects Mousepad properties and records an `UnknownMethod` fallback. Exporter interaction therefore remains uncredited. Topbar/dock geometry passes the retained resolution matrix, and AT-SPI exposes named shell surfaces and a focused Search field; broader hardware and locale coverage remain open. |
| X11 window-management integration | 10 | **9/10** | Openbox, X11 margins, four desktops, launcher hotkey, deliberate move/resize and Alt+Tab focus switching pass the Docker smoke path; disposable Xvfb checks pass at 1366x768, 1920x1080, 3440x1440, 3840x2160, 5120x2880 and 7680x4320 at scale 1 plus 2560x1600 at GDK_SCALE=2. The pinned installed-to-disk VirtualBox guest boots Xorg/Openbox and passes in-guest shell/window QA at 1280x800, but that guest predates the verified milestone; high-refresh physical timing, exhaustive drag/resize and hardware-display coverage remain open. |
| Upstream application integration | 10 | **9/10** | The Arch/X11 matrix launches five distinct upstream roles—PCManFM, Xfce Terminal, Mousepad, Ristretto and browser—with fresh 1280x800 scenes. The current cache gate captures Chromium through the no-fork X11 wrapper, then launches SuperTux on a packaged world1 level, sends movement/jump input while the live window is verified, observes its PulseAudio sink input, captures non-silent raw PCM from the sink monitor, and exits through the game's own input path. The matrix now installs the same SLOPOS Openbox `themerc` and explicit config used by the canonical Docker scenes, so its upstream frames are evidence of the intended window chrome rather than the distro default. Firefox is an optional package leg and was skipped in the current provisioned image (`BROWSER_FIREFOX_STATUS_SKIPPED_OPTIONAL_PACKAGE`). Chromium inherits the SLOPOS GTK/XDG identity and optional Platinum frame theme; the Firefox profile integration remains source-level/optional evidence. Physical audio/GPU output and full upstream-window visual convergence remain unreviewed. |
| Software Catalogue / AppImage | 8 | **8/8** | Metadata now requires complete control-free fields, HTTPS host URLs, trusted SHA-256 and a valid ELF payload; redirects are HTTPS-only and bounded. The four curated entries (Kdenlive 24.05.0, Inkscape 1.3.2, GIMP 3.2.4 and Audacity 3.7.7) are installable only with pinned official assets/digests; OBS is omitted because its official release has no Linux AppImage. |
| System services integration | 8 | **6/8** | Settings is an honest upstream-tool hub and shell status reads available NetworkManager/PipeWire/battery state. A disposable AT-SPI service-boundary run proves all eight controls are disabled when their utilities are absent and that all eight available panels invoke their upstream delegates; hardware-backed mutation, suspend and Bluetooth behavior remain unverified. |
| Installer / boot / session | 8 | **7/8** | Arch/Debian/ISO/root manifests contain only the four binaries and required assets; disposable Ubuntu smokes validate custom-prefix and privileged default-prefix installs. The rebuilt Arch ISO contains UID-1000 `slopos`, executable session binaries, primary LightDM autologin/session keys, and a 1280x800 QEMU boot reaches the SLOPOS top bar and launcher after LightDM. The installed-to-disk VM verifies the on-disk session handoff for pinned SHA `206c456e`, not the current tree; EFI diversity and installer recovery remain open, so one point is withheld. |
| Functional QA | 7 | **7/7** | The current Rust gate passes metadata, fmt, check, 14 catalogue tests, 14 shell unit tests, 29 shell integration tests, 3 spaces tests and 1 Settings test with zero failures; Docker/Xvfb passes all eight stages with recovery, deliberate overlap/focus checks and mandatory screenshots. A separate Docker/Xvfb click-through downloaded Kdenlive, verified its 204,903,616-byte payload hash and desktop entry. The QEMU ISO guest reaches the live SLOPOS session at 1280x800, and the pinned installed-to-disk guest passes its in-guest QA; the VM result predates the verified milestone, and hardware behavior remains open. |
| Visual regression QA | 5 | **3/5** | Canonical desktop, application, multi-window, Catalogue and Settings scenes are captured and manually inspected at 1280x800; retained desktop/Search/Settings scenes now also pass exact-dimension checks at 1366x768, 1920x1080 and 2560x1600 at GDK_SCALE=2. The contract's independent 95/100-per-scene visual review is not complete. |
| Performance | 3 | **2/3** | Three independent disposable Ubuntu launches per resolution recorded `scripts/benchmark-x11-session.sh` startup of 293-567 ms and process-tree RSS of 97,188-121,556 KiB at 1280x800, plus 294-820 ms and 100,764-121,316 KiB at 1920x1080. A fresh 30-second 1280x800 hold now kept Openbox, the shell, topbar and session supervisor alive with RSS delta 128 KiB under the 32,768 KiB bound; true cold-cache and hardware-target budgets remain unverified. |
| Accessibility / localization | 3 | **2/3** | Ubuntu/Xvfb AT-SPI acceptance finds six named SLOPOS surfaces and focused Search. Extended runs at 1280x800/GDK_SCALE=1 and 2560x1600/GDK_SCALE=2 verified exact UTF-8 `café` text plus reversible Tab/Shift+Tab focus (`AT_SPI_EXTENDED_STATUS_0`); fresh `fr_FR.UTF-8` and `de_DE.UTF-8` matrix legs pass with generated locale aliases, and an Orca-backed run emits speech-output/debug evidence for the focused Search field (`AT_SPI_SCREEN_READER_ORCA_STATUS_0`). Hardware accessibility remains open. |
| Recovery / resilience | 3 | **2/3** | The Docker gate proves one restart of each child; a separate disposable run completed three consecutive shell and Openbox kill/respawn cycles with fresh PIDs, visible shell windows and the supervisor still alive. Hardware/VM recovery remains unverified, so one point is withheld. |
| **Total** | **100** | **78/100** | **Verified development milestone; not production-ready.** |

## Current release-blocking findings

1. **False completion accounting:** `TRUTH.md` awarded 100/100 despite visibly unfinished canonical screenshots and unverified claims.
2. **Release evidence:** a 1,974,534,144-byte Arch ISO now builds reproducibly, was booted through LightDM in QEMU, and was installed to a fresh 40 GiB VirtualBox disk. The installed guest reported its pinned source SHA (`206c456e`) and passed `qa-vm.sh` at 1280x800; this VM run predates the verified milestone `c74ce88`, and physical hardware, EFI diversity and installer recovery paths remain open.
3. **Visual acceptance:** all eleven contract scene types now have fresh 1280x800 captures and have been manually inspected; retained 1366x768, 1920x1080 and 2560x1600/GDK_SCALE=2 scenes now have exact-dimension evidence, but the independent 95/100 scene gate remains open.
4. **System and quality evidence:** hardware-backed service mutation, suspend/Bluetooth behavior, hardware accessibility, true cold-cache and hardware-target performance budgets remain open; named-surface AT-SPI, UTF-8 input, reversible focus traversal, generated `fr_FR.UTF-8`/`de_DE.UTF-8` locale legs, Orca speech-output/debug startup, HiDPI scaling, honest unavailable controls, bounded long-run liveness and bounded child-failure recovery are now evidenced.
5. **Packaging policy:** privileged default-prefix/custom-prefix installs verify the X11 descriptor and absolute `Exec`/`TryExec` paths; the installed VirtualBox guest also verifies the on-disk four-binary session handoff. EFI diversity and installer recovery remain open.
6. **Browser/game boundary:** the current Arch cache gate passes deterministic local browser DOM/visible-title checks and a visible PID-matched X11 frame for Chromium, plus a real SuperTux `world1/frosted_fields.stl` level with movement/jump input, live-window checks, a non-silent PulseAudio monitor capture and a clean process exit. The Firefox leg is optional and was skipped because the provisioned image has no Firefox package; its disposable profile/theme path is not current runtime evidence. The installed `slopos-browser.desktop` entry routes HTML/HTTP(S), Openbox and Search launches through the X11/GTK wrapper; raw upstream browser desktop entries are also routed through it when available, while the wrapper preserves the selected upstream engine through `SLOPOS_BROWSER`. A browser can only inherit the SLOPOS GTK/frame language through upstream extension/profile mechanisms. Physical game audio, GPU behavior and complete browser-content styling still require hardware/independent review.
7. **Global application menus:** the generic focused-window command proxy has been removed. SLOPOS detects `_GTK_UNIQUE_BUS_NAME` and `_GTK_APP_MENU_OBJECT_PATH` on focused X11 windows and imports bounded `com.canonical.dbusmenu` layouts only when the App button is explicitly invoked. It renders only visible/enabled protocol items and sends only `Event(..., "clicked", ...)`; malformed responses, absent buses and timeouts remain local-menu fallbacks. The full Docker harness now compiles a disposable QA-only exporter when `libdbus-1` is available and can require a real `GetLayout`/`Event` click-through with `SLOPOS_QA_REQUIRE_REAL_APPMENU=1`; this source/harness path is not yet rerun after the cache was removed. Existing evidence records Mousepad advertising the standard properties and a real App-button attempt failing closed with `UnknownMethod`; the cache-only `scripts/run-appmenu-qa.sh` remains synthetic. Parser tests cover standard/separator/submenu layouts; no successful end-to-end `GetLayout`/`Event` result is currently credited.
8. **Resolution and refresh boundary:** cached Xvfb geometry evidence passes 3440x1440, 3840x2160, 5120x2880 and 7680x4320 at scale 1, alongside the retained 1366x768, 1920x1080 and 2560x1600/GDK_SCALE=2 checks. This proves screen-relative allocation and image dimensions, not physical 120/144/240Hz timing, VRR, GPU bandwidth or 5K/8K hardware output.

## Canonical visual findings

The 2026-08-13 Docker/Xvfb screenshots are retained as fresh evidence of the baseline, not proof of acceptance. The latest pass fixed transient blank clean-desktop captures, tightened the Settings hub layout, and gave the Application Strip an explicit Platinum “Apps” gutter and stronger inset shelf; remaining review areas include:

- Settings uses compact, consistently framed delegated control panels with honest unavailable states and eight packaged Platinum SVG icons with symbolic fallbacks;
- application strip now has a clearer “Apps” identity and inset Platinum shelf, but still needs independent classic-launcher review at the retained resolutions;
- notification and About/confirmation dialogs now use the packaged SLOPOS mark with compact Platinum framing, but their composition still needs independent classic-Mac review;
- the notification toast now starts at y=60, below the 26px Openbox margin, 26px top bar and an 8px gap; the fresh scene no longer covers the menu/status controls;
- Search now uses packaged Platinum role icons for the known file manager, terminal, text editor, browser, image viewer, Catalogue, Settings and desktop roles; unrelated desktop entries retain their upstream icon so the shell does not misrepresent unknown applications. Its default 560x450 palette reserves a 280px scroll viewport, and the fresh 1280x800 scene keeps every initially visible result row fully inside the frame;
- the launcher scrollbar is now persistent and Platinum-framed, and the active-app capture uses a PID-matched Mousepad window instead of duplicating the file-manager scene; independent visual scoring is still required before awarding additional points;
- GTK HeaderBar/CSD presentation in Catalogue and Settings conflicting with Openbox/System 7 chrome;
- file manager/terminal retaining upstream window identity, although native GTK menubar spacing now inherits the Platinum theme and no longer collapses labels;
- weak active/inactive hierarchy and limited classic frame vocabulary;
- missing independent review of the now-captured menus, launcher, notification/dialog states, disabled states, check/radio/slider/scrollbar states and HiDPI scenes.
- the top bar no longer paints a fabricated File/Edit/View/Window/Help proxy over upstream applications; its disabled App status reports no X11 AppMenu exporter, while an advertised exporter enables the bounded importer and falls back to the Mousepad/terminal/browser local menu if the DBus object is absent or malformed. The current Docker evidence records property detection plus an `UnknownMethod` fail-closed attempt, not successful imported layout/action delivery.

## Acceptance accounting rules

- A passing build is not visual acceptance.
- Xvfb screenshot generation is not visual acceptance.
- A control receives functional credit only if the selected value is applied to real system state or the control honestly delegates to an upstream settings utility.
- AppImage installation receives credit only for HTTPS download + mandatory trusted digest/signature validation + atomic placement + desktop integration. No synthetic fallback payloads are allowed.
- Visual credit requires review of real screenshots at required resolutions, not source inspection.
- No domain receives full credit while its canonical scene or functional scenario has a known release-blocking defect.
- `100/100` requires a bootable/installable release candidate plus independent functional and visual evidence.

## Verified remediation tranche

The 2026-08-11 deep audit initiates a corrective tranche that:

- makes catalogue metadata, redirects and AppImage payload validation fail closed;
- converts Settings to an honest upstream-tool hub and reports unavailable global commands;
- keeps the existing shell instance for the launcher hotkey;
- removes stale Wayland/compositor assumptions from shipping manifests, CI and notices;
- adds current X11 package/asset/prefix contract tests;
- fixes CRLF-safe Arch ISO package-list normalization and adds the missing Arch GTK/X11 build libraries;
- replaces Catalogue missing-icon placeholders with a packaged Platinum fallback mark;
- bounds the packaged identity mark and restores the canonical slate desktop after Openbox starts;
- strengthens Docker/VM QA with fresh visible windows, launched-PID checks and mandatory non-empty screenshots;
- adds the gdk-pixbuf loader dependency and eight packaged Platinum SVG icons for the Settings hub.

## Post-3287d00 source tranche (runtime verification pending)

- `scripts/slopos-recovery.sh` now preserves per-user SLOPOS/Openbox configuration in a collision-safe backup, stages optional vendor defaults, keeps `slopos-session` alive when it is already supervising the desktop, starts a supervisor only when absent, waits for fresh Openbox/shell PIDs and emits `SLOPOS_RECOVERY_STATUS_0` only after both children return.
- `packaging/vm/arch-install.sh` now handles both `/dev/sda` and NVMe partition naming and unmounts `/mnt` through an EXIT cleanup trap. This is source/syntax hardening, not current-tree VM evidence.
- The active Openbox titlebar theme now uses a smooth Platinum bevel gradient instead of dense interlacing; the source contract records the intended active/inactive hierarchy. A fresh visual review is still required before changing the visual score.
- `scripts/run-docker-qa.sh` now carries `scripts/qa-dbusmenu-exporter.c`, a QA-only low-level `com.canonical.dbusmenu` service that returns one bounded action and logs its protocol `Event`. The fixture is compiled into `/tmp`, never installed, and is optional unless `SLOPOS_QA_REQUIRE_REAL_APPMENU=1` is set. No post-3287d00 Docker/Xvfb run is claimed here because the local image cache is currently empty.

## Post-d6d25d7 source hardening (runtime verification pending)

- `scripts/run-resolution-qa.sh` now keeps screenshot filenames underscore-safe while writing the default evidence directory as `WIDTHxHEIGHT-scaleN`, matching the CI upload matrix. The previous mismatch could let a successful geometry job lose its artifacts; this fix is source-level until the hosted matrix reruns.
- `scripts/install-browser-theme.sh` moves an existing Chromium unpacked theme to a unique backup instead of recursively deleting it, and normalizes Firefox's stylesheet preference to one explicit `true` declaration after backing up `user.js`. This preserves upstream browser profiles while keeping the no-fork integration opt-in.
- `packaging/vm/provision.ps1` verifies the exact installer SHA-256 before execution, uses fail-closed HTTP/syntax checks and `pipefail`; `packaging/vm/qa-vm.sh` requires UEFI runtime/loader evidence, checks both possible Wayland-session locations, and refuses to skip the pinned source-contract scan. `scripts/install-session-files.sh` rejects malformed source descriptors missing `Exec=` or `TryExec=`.
- Static Bash/PowerShell/YAML checks for this tranche pass in the current worktree. No current-tree cargo, Docker/Xvfb, EFI, physical-monitor or hardware evidence is claimed here; the score remains 78/100 until those gates produce fresh results.

## Evidence ledger

- `docker run --rm -v <repo>:/workspace -w /workspace ubuntu:24.04 bash /workspace/scripts/run-docker-qa.sh` completed all eight stages, including shell/Openbox child-restart checks, with `SLOPOS-I Docker/Xvfb functional evidence PASS` and exit 0 in a fresh Ubuntu 24.04 snapshot.
- Hosted CI run [#568](https://github.com/palaashatri/rust-slopos/actions/runs/31754694669) for `63de816` has a succeeded build/test/lint job; its current source reports 14 catalogue, 14 shell unit, 29 shell integration, 3 spaces and 1 Settings test, with 0 failures. The remaining locale/resolution matrix was still in progress when this ledger was updated, so the prior completed full-run evidence remains separately identified below.
- The official Audacity 3.7.7 release asset was downloaded from `https://github.com/audacity/audacity/releases/download/Audacity-3.7.7/audacity-linux-3.7.7-x64-22.04.AppImage`; its local SHA-256 was `45c4445fb6670cc5fe40d31c7cea979724d2605bca53b554c32520acbf901ef0`, matching the release-published checksum.
- The official KDE archive serves `https://download.kde.org/Attic/stable/kdenlive/24.05/linux/kdenlive-24.05.0-x86_64.AppImage` and publishes SHA-256 `b2ea1c3cc5af7eda58c5a19bfd35cde9a050fb70c5f2526117c9cc69a46576f0` in its mirrorlist.
- The official GIMP v3.2 archive serves `https://download.gimp.org/gimp/v3.2/linux/GIMP-3.2.4-x86_64.AppImage` and publishes SHA-256 `f1ce6dc671ef1c4aad87a0db9d7462e8ca9c0b5f899456337803c6ba32d0babe` in `SHA256SUMS`; the official Inkscape 1.3.2 media asset was downloaded as 125,195,456 bytes and independently rehashed to `351deaea3fa391c56e0c6401dadcf83f7c9c8f82faa47bdb07024e99b92f9b5c`.
- OBS is not seeded because the official 30.1.2 release does not provide a Linux AppImage; no third-party substitute is used.
- A disposable Ubuntu/Xvfb click-through installed Kdenlive from the Catalogue: the resulting executable was 204,903,616 bytes, its SHA-256 matched `b2ea1c3cc5af7eda58c5a19bfd35cde9a050fb70c5f2526117c9cc69a46576f0`, and `slopos-appimage-kdenlive.desktop` was created.
- A disposable Ubuntu full-install smoke with `XSESSION_DIR` and `--prefix` installed all four binaries, `start-slopos-i`, the X11 descriptor, Openbox/GTK assets, mimeapps and the SLOPOS logo; descriptor `Exec`/`TryExec` both resolved to the custom prefix (`INSTALL_LAYOUT_STATUS_0`, exit 0).
- A disposable Ubuntu 24.04 container ran the default-prefix installer as root with `--no-deps --no-build --distro ubuntu`; all five installed executables were present, `/usr/share/xsessions/slopos-i.desktop` was non-empty, its `Exec` and `TryExec` both resolved to `/usr/local/bin/slopos-session`, and no Wayland session descriptor was installed (`INSTALL_DEFAULT_PREFIX_STATUS_0`).
- An Arch Linux `archiso` container built the live ISO after installing the declared Arch GTK/X11 build libraries; `mkarchiso` completed all six stages and produced `artifacts/iso/archlinux-2026.08.12-x86_64.iso` (1,974,534,144 bytes, SHA-256 `41c69ad74422b9e2dca69e7bb658da8f051d25e80b5bbc30d0de41c03feb10f0`, `ISO_BUILD=0`). This earlier media-build record is retained for provenance; the later ISO hash and installed-VM evidence below are the current release evidence.
- The fresh Catalogue scene now renders the original Platinum software mark for all four verified rows instead of GTK missing-icon placeholders; the source contract and full Docker/Xvfb gate pass after the change.
- Disposable Xvfb geometry checks passed at 1366x768 and 1920x1080: the topbar reported full screen width, the Application Strip remained centered, and `scrot`/ImageMagick reported exact matching screenshot dimensions.
- `scripts/run-resolution-qa.sh` passed in disposable Ubuntu/X11 environments at 1366x768/GDK_SCALE=1, 1920x1080/GDK_SCALE=1 and 2560x1600/GDK_SCALE=2. Each run reported full-width topbar geometry, a centered Application Strip and non-empty desktop/Search/Settings PNGs with exact matching dimensions; the HiDPI run exercised the logical-coordinate conversion that keeps the strip centered.
- Cache-only `scripts/run-resolution-qa.sh` runs passed at 3440x1440, 3840x2160 (4K), 5120x2880 (5K) and 7680x4320 (8K), each reporting exact desktop/Search/Settings dimensions, full-width topbar geometry and a centered Application Strip. These runs reused the provisioned Arch X11 image and existing release binaries; no package mirror or toolchain download was performed. High-refresh physical monitor timing remains unverified.
- Cache-only `scripts/run-appmenu-qa.sh` passed with `Mousepad local menu remains upstream-owned`, `NON_EXPORTER_STATUS_0`, `EXPORTER_FIXTURE_STATUS_0`, `NON_EXPORTER_RETURN_STATUS_0` and `SLOPOS AppMenu capability evidence PASS`. The fixture only sets the standard X11 properties; it provides no DBusMenu service and does not invoke the App button. The current full Docker session separately logged Mousepad's real AppMenu property advertisement and a bounded App-button attempt that failed closed with `UnknownMethod`; no successful `GetLayout`/`Event` result was captured. This is capability/fallback evidence while Rust tests exercise bounded standard/separator/submenu layout parsing and protocol-only Event dispatch.
- The CI workflow now exposes a manual `workflow_dispatch` `x11-appmenu-real` job that provisions the disposable C exporter, requires `APPMENU_REAL_IMPORT_STATUS_0`, and uploads the exact-commit log/screenshot bundle. It has not been dispatched for the current tree, so it is a reproducibility path—not current AppMenu readiness evidence or a score increase.
- A separate disposable Xvfb session completed three consecutive shell and Openbox kill/respawn cycles; each cycle produced a fresh PID and visible topbar/dock, and the supervisor remained alive (`RECOVERY_REPEAT_STATUS_0`, exit 0).
- Three independent disposable Ubuntu launches per resolution produced `BENCHMARK_STATUS_0`: 1280x800 startup/RSS pairs `(549 ms, 121,556 KiB)`, `(567 ms, 118,996 KiB)`, `(293 ms, 97,188 KiB)`; 1920x1080 pairs `(565 ms, 120,712 KiB)`, `(294 ms, 100,764 KiB)`, `(820 ms, 121,316 KiB)`. These are Xvfb process-tree measurements, not hardware or long-run budgets.
- A disposable Ubuntu smoke install of `scripts/install-session-files.sh --prefix /workspace/artifacts/qa/prefix-test --session-dir /workspace/artifacts/qa/xsessions-test` produced an X11 descriptor whose `Exec` and `TryExec` both point to the prefix's `bin/slopos-session`.
- The current Arch ISO build completed with `mkarchiso` in a privileged Arch container. Its SHA-256 is `318ffc19ee0289c0e9c7d279ee34113546ff926065ef0d7685384e37aa6afa22`; extracted squashfs checks show UID-1000 `slopos`, mode-755 SLOPOS session binaries, primary LightDM autologin/session keys and no leftover customization hook. QEMU booted that ISO through LightDM into the SLOPOS top bar and launcher; the retained 1280x800 screenshot is `artifacts/qa/iso-boot-final-360s.png` (PNG IHDR verified as 1280x800). The QEMU helper's exact window-name probe did not emit a machine PASS, so this is visual live-ISO evidence, not installed-VM acceptance.
- The current cache-only Arch upstream application gate launched PCManFM, Xfce4 Terminal, Mousepad, Ristretto and Chromium through the installed `slopos-browser.desktop`/`start-slopos-browser` path, then launched SuperTux on the packaged `world1/frosted_fields.stl` level at 960x540. Each launched window matched its PID; six fresh 1280x800 PNGs were non-empty, and the game leg reported `game_scene_mean=0.606418`, `game_audio_bytes=1723524`, `nonzero_audio_bytes=1601346` before exiting through its own input path. The provisioned image has no Firefox package, so the gate emitted `BROWSER_FIREFOX_STATUS_SKIPPED_OPTIONAL_PACKAGE`; Firefox remains an optional package leg rather than current evidence. This remains bounded container evidence, not physical speaker or GPU evidence.
- A fresh disk-only VirtualBox boot of the installed Arch guest matched pinned commit `206c456e0ae02a5a9543ff15fce413618f222fd8`; `qa-installed.ps1` captured `qa_exit=0`, `screenshot_exit=0`, `passed=true`, and the guest emitted `SLOPOS_X11_INSTALLED_VM_QA=PASS` with `shipping source contract clean (16 files)`. This provenance predates the verified milestone `c74ce88`, so it is installation evidence for the pinned SHA, not current-tree VM evidence. The generated `artifacts/qa/installed-vm-evidence/status.json`, `qa-vm.log` and screenshot are local ignored evidence, not source-controlled release artifacts.
- The current cache-only Arch application gate provides Chromium's no-fork X11 wrapper/theme path, deterministic DOM marker and fresh visible frame; its Firefox log records that the package is absent and the optional runtime leg was skipped. The wrapper clears `WAYLAND_DISPLAY`, forces GTK/Firefox to X11 and adds Chromium-family `--ozone-platform=x11`; it never rewrites a browser binary or web content. The optional theme covers Chromium toolbar/omnibox colors and Firefox toolbar buttons, tabs, panels, sidebar and find bar while leaving the engine upstream. A Firefox runtime frame and container-only sandbox warning require a separately provisioned Firefox leg; the current gate does not provide them. The Figma Classic Macintosh UI Kit supplied by the owner was captured and used as the visual comparison reference; upstream browser chrome remains only best-effort aligned.
- The retained Chromium PNG in `artifacts/qa/app-matrix/browser.png` predates the latest source-only toast-dismissal loop and visibly includes Chromium's transient “Installed theme” banner. It is therefore not clean browser visual evidence; a fresh Arch/X11 run after `aa72d88` must confirm the banner is gone before browser-frame polish receives additional visual credit.
- The shared GTK theme now styles native `menubar > menuitem` spacing, hover and focus states. Fresh PCManFM and Xfce4 Terminal captures show separated Platinum menu labels; this same GTK surface is available to browser-native dialogs/menus through the no-fork wrapper, while browser tab, address-bar and web-content styling remain engine/profile-owned.
- Canonical screenshot capture now removes every prior PNG, moves the pointer away from transient tooltips, uses scrot overwrite mode and gives the notification a 60-second QA lifetime, preventing stale or pointer-raced images from satisfying a later run; the fresh Docker/Xvfb gate produced eleven expected base-named 1280x800 captures for every contract scene, including a visible D-Bus notification and functional About modal. The scene set is evidence for review, not an automatic visual score.
- The current Docker/Xvfb harness closes the captured notification and asserts that no visible notification remains before launching Catalogue and Settings; the fresh Settings and Catalogue captures are therefore isolated from the notification scene.
- A fresh Docker/Xvfb pass after the Application Strip refinement completed all eight stages with exit 0; the accepted `clean_desktop_1280x800.png` visibly includes the “Apps” gutter, nine launcher positions (including an optional SuperTux control) and the centered strip. The game control disables itself when neither `supertux2` nor `supertux` is installed, preserving the no-fake-functionality rule.
- The same refinement passed fresh retained-resolution runs at 1366x768/GDK_SCALE=1, 1920x1080/GDK_SCALE=1 and 2560x1600/GDK_SCALE=2. Each run reported `RESOLUTION_QA_STATUS_0`, exact screenshot dimensions, full-width topbar geometry and centered strip coordinates (`700`, `977`, `1314` respectively).
- The latest Docker/Xvfb pass waits for the recovered top bar and Application Strip before the clean scene, and its multi-window scene explicitly moves/resizes the terminal, exercises Alt+Tab, then restores terminal focus. The Settings capture uses the compact 640x460 control-panel layout and disables unavailable delegated utilities.
- The current post-icon Docker/Xvfb pass completed all eight stages with exit 0; `artifacts/qa/screenshots/system_settings_1280x800.png` visibly renders all eight packaged Platinum panel icons at 32px with unavailable utilities disabled.
- The current post-dialog Docker/Xvfb pass completed all eight stages with exit 0; `artifacts/qa/screenshots/modal_about_1280x800.png` visibly renders the packaged SLOPOS mark in a compact Platinum dialog, with no generic GTK lightbulb/question artwork.
- The current Search Docker/Xvfb scene renders packaged folder, desktop, text-editor and image-viewer role icons while preserving upstream icons for unrelated applications; the source contract and full gate pass with exit 0.
- The current Search Docker/Xvfb scene uses the expanded 560x450 palette and shows eight complete initial result rows with no bottom-row clipping; the source contract (`launcher_default_geometry_keeps_result_rows_fully_visible`) and full gate pass with exit 0.
- The current-tree Rust gate returned `FMT_STATUS_0`, `CHECK_STATUS_0`, `TEST_STATUS_0` and `CLIPPY_STATUS_0`; the release workspace build returned `RELEASE_BUILD_STATUS_0`. The verified current source now has 14 catalogue, 14 shell unit, 29 shell integration, 3 spaces and 1 Settings test. Cache-only AppMenu QA returned `APPMENU_QA_STATUS_0`, full Docker/Xvfb returned `DOCKER_QA_STATUS_0`, and the Arch app/game gate returned `ARCH_APP_QA_STATUS_0` with Chromium status 0 and Firefox explicitly skipped because it is absent from the provisioned image; a fresh post-`63de816` Arch runtime rerun is still unavailable because the local cache is empty.
- The latest visual-evidence batch completed the full Ubuntu 24.04 Docker/Xvfb gate with exit 0 after adding a non-overlay launcher scrollbar, below-topbar notification placement, PID-matched Mousepad active-app capture and stale-window cleanup; fresh `active_app_1280x800.png` and `file_manager_1280x800.png` hashes differ (`018BFD82...` vs `D11668EE...`). The independent visual gate remains open, and the capability-aware global-menu fallback keeps the current score at 78/100 after conservative Installer/Recovery accounting.
- The isolated Ubuntu/Xvfb AT-SPI gate starts a D-Bus accessibility registry, launches the current shell, Settings and Catalogue, toggles the existing Search singleton, and verifies six named surfaces plus focused Search (`AT_SPI_EXPECTED_NAMES=6`, `AT_SPI_STATUS_0`). Extended runs at 1280x800/GDK_SCALE=1 and 2560x1600/GDK_SCALE=2 type and read back UTF-8 `café`, then verify Tab and Shift+Tab focus reversal (`AT_SPI_EXTENDED_STATUS_0`). The acceptance script is wired as a dedicated CI job; hardware accessibility and independent locale/user-review coverage remain open.
- The disposable Settings service-boundary acceptance starts two clean Xvfb/D-Bus sessions: with an empty utility path it observes all eight named control panels disabled (`SETTINGS_UNAVAILABLE_CONTROLS_DISABLED=8`), and with temporary upstream command stubs it observes every panel enabled and invokes all eight delegates through their AT-SPI actions (`SETTINGS_DELEGATED_CONTROLS=8`, `SETTINGS_DELEGATED_DISPLAY=arandr`, `SETTINGS_SERVICE_QA_STATUS_0`). This proves honest delegation and absence handling, not real hardware mutation, suspend or Bluetooth behavior.
- The nested D-Bus shells used by the AT-SPI and Settings service QA now install inner EXIT traps for every child PID, so assertion failures cannot strand the session, accessibility bus or utility probes on a CI runner. Static shell contracts and the current cache-only gates pass after this cleanup fix.
- The AT-SPI runner now provisions and validates requested UTF-8 locales before starting the session. Fresh disposable matrix runs passed `fr_FR.UTF-8` and `de_DE.UTF-8` (runtime alias `de_DE.utf8`), each reporting `AT_SPI_EXPECTED_NAMES=6`, `AT_SPI_UTF8_TEXT=café`, `AT_SPI_EXTENDED_STATUS_0`, `AT_SPI_STATUS_0` and the requested `AT_SPI_LOCALE`; CI runs both matrix legs. This is locale/accessibility evidence, not screen-reader or hardware-accessibility evidence.
- The optional `SLOPOS_ATSPI_SCREEN_READER=1` mode installs Orca and starts it on the same D-Bus/AT-SPI session. A fresh Ubuntu run emitted `AT_SPI_SCREEN_READER_ORCA_STATUS_0` after finding Orca startup, speech-output debug lines and `Application search field` while the regular six-surface/UTF-8/focus checks passed. This is a headless screen-reader smoke, not hardware accessibility or speaker-output acceptance.
- The current benchmark hardening uses owned session/shell/Openbox/Xvfb PIDs, rejects zombie-window-manager matches, retries transient X11 window queries and enforces a configurable RSS-growth limit. A clean Ubuntu run held 1280x800 for 30 seconds with `SESSION_TREE_RSS_KIB_INITIAL=114936`, `SESSION_TREE_RSS_KIB_FINAL=115064`, `SESSION_TREE_RSS_DELTA_KIB=128`, `BENCHMARK_HOLD_SECONDS=30` and `BENCHMARK_STATUS_0`; a separate 1920x1080 run held for 5 seconds with delta 128 KiB. This is disposable Xvfb evidence, not cold-cache or hardware-target evidence.
- `cargo fmt --all -- --check` passes in the Rust 1.97 container after the remediation formatting pass.
- GitHub Actions CI run [#566](https://github.com/palaashatri/rust-slopos/actions/runs/31754087286) for commit `9c073d8` completed successfully in 3m15s. Its hosted Linux build/test/lint, Xvfb/Openbox smoke, AT-SPI surfaces, Orca, Settings delegation, bounded benchmark, release build, two locale legs and five resolution legs all completed; artifact/cache upload warnings did not change the job result. This is current hosted CI evidence, not a replacement for current-tree VM, physical hardware or independent visual review.
- `git diff --check` and `bash -n` for the changed installer, session, Docker, ISO and VM scripts pass.
- The Docker gate intentionally prints that visual acceptance remains a separate human/vision review gate; this ledger does not turn generated screenshots into a 100/100 claim.
